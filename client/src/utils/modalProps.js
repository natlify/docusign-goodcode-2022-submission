export const modalProps = {
  centered: true,
  styles: (theme) => ({
    modal: {
      width: 600,
      backgroundColor: theme.colors.dark[4],
    },

    title: {
      textTransform: "uppercase",
      fontWeight: 700,
      fontSize: "20px",
      letterSpacing: "2px",
    },
  }),
};
