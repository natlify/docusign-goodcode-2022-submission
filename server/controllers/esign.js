import * as docusign from "../services/@docusign.js";

export default class eSignController {
  static createEnvelopeForSigning = async (req) => {
    // const cameraData = await docusign.querySurvey123ByCamera(
    //   req.params.cameraId,
    // );
    // if (!cameraData)
    //   throw new Error("No matching Camera details found. Please check your ID");
    return "Hello World";
  };
}
